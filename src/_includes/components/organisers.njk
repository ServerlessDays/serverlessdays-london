<div class="flex-ns mv3 mv4-l">
    <div class="w-100 w-05-l dn db-l"> 
    </div>
    <div class="w-100 w-50-l ph3 ph0-ns tc tl-ns pb4-l">
        <h2 class="f2 mv3">Organisers</h2>
        <div class="f5 lh-copy pr3-m">{% markdown organisers.text %}</div>
        <div class="pt2 pt3-ns">
            {% for organiser in organisers.people %}
                <a class="no-underline" href="{{ organiser.link }}" alt="{{ organiser.alt }}" >
                    <img class="w4 h4" src="{{organiser.img}}" alt="{{ organiser.alt }}">
                </a>
            {% endfor %}
        </div>
    </div>
    <div class="w-100 w-10-l dn db-l">
    </div>  
</div>